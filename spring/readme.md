
```bash
spring init \
  --build=maven \
  --language=java \
  --boot-version=3.1.4 \ # 根据需要调整版本号
  --dependencies=web,jpa,h2,thymeleaf \
  --package-name=com.example.demo \
  my-springboot-app
```

```bash
spring init --language=java --build=maven --boot-version=3.3.0 --dependencies=web --package-name=com.ylz.server server

```
